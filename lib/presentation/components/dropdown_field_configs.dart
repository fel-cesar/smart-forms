import 'package:flutter/material.dart';

class DropdownFieldConfigs extends StatelessWidget {
  const DropdownFieldConfigs({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
